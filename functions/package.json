{
  "name": "weatherbot-app-functions",
  "description": "weatherbot-app Cloud Functions",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google/genai": "^2.8.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^7.2.5",
    "google-auth-library": "^9.15.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "~5.8.2"
  }
}
